import * as rls from 'readline-sync' ;

let nroDeseado: number = rls.questionInt ("escriba el numero que desea verificar:");
//hacer esto
if (nroDeseado %2 ==0) {
        console.log ('el numero es par:' + nroDeseado);
        // sino esto 
}else { (nroDeseado) 
        console.log ('el numero es impar:' + nroDeseado)};
